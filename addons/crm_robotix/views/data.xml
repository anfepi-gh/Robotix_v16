<odoo>

    <record id="ir_cron_crm_lead_update_invoice_target_automatic" model="ir.cron">
        <field name="name">Actualización Objetivo de Facturación Distribuido (CRM)</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="doall" eval="False"/>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="code">model._automatic_update_invoice_target()</field>
        <field name="state">code</field>
    </record>

</odoo>